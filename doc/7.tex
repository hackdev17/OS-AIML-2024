\documentclass{article}

%!TEX program=xelatex

% packages
\usepackage[a4paper,margin=.5in]{geometry}
\usepackage{minted}
\usepackage{enumitem}
\usepackage{verbatim}

% prestes
\pagestyle{empty}

\begin{document}
	\noindent 1. Develop a C program to implement the process system calls \verb|fork()|, \verb|wait()| to create process and terminate process.

	\inputminted{c}{lab1.c}
\end{document}